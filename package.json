{
  "name": "illusions-ruleset-gendai-kanazukai",
  "version": "0.3.2",
  "private": false,
  "description": "illusions 校正ルールセット — 現代仮名遣い（昭和61年内閣告示第1号）。",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsup",
    "build:min": "tsup --minify",
    "validate": "node scripts/validate.mjs",
    "check": "npm run validate && npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
